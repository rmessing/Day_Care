
<div class="title_center"> Attendance </div>

<div class="reports">

	<% @children.each do |child| %>
		<br>

		<%= child.fname %>
		<%= child.mname %>
		<%= child.lname %>

		<br>
		<% child.handoffs.each do |handoff| %>

			<%= handoff.attend %>
			<%= handoff.created_at %>
			<br>
		<% end %>
		<br>
	<% end %>
</div>

<div class="center_div">
	<%= button_to "Center Home", center_path(@center), method: :get %>
</div>