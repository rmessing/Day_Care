<div class="title_center">
	<H1> Select a Class </H1>
</div>

<div class="reports">

	<% @groups.each do |group| %>

		<a href="<%= edit_group_path(group.id) %>"
			<h3><%= group.name %></h3>
		</a>
		<br>
		
	<% end %>
	<br>
	<%= button_to "Center Home", center_path(@center), method: :get %>
</div>

